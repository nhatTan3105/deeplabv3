{
     "builds": [
         {
             "src": "./app.py",
             "use": "@vercel/python"
         }
     ],
    {
    "rewrites": [
         { "source": "/(.*)", "destination": "/" },
      { "source": "/detect(.*)", "destination": "/detect" }
    ]
  }
 }

